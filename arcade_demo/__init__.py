"""arcade_demo — a standalone arcade-mcp two-axis fail-closed tool example.

NOT part of the installable `tripwire` wheel (see pyproject `[tool.hatch.build.targets.wheel]`,
packages = ["tripwire"]). This `__init__.py` only makes the directory importable as
`arcade_demo.*` for the smoke test.
"""
